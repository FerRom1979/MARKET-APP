/* eslint-disable no-use-before-define */
import React from 'react';
import SideMenu from './Components/SideMenu';

const App: React.FC = () => {
  return (
    <div className="App">
      <SideMenu />
    </div>
  );
};

export default App;
