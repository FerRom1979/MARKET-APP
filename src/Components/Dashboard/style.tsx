import { makeStyles } from '@material-ui/core/styles';

const usesStyles = makeStyles(() => ({
  root: {
    display: 'flex',
  },
  container: {
    paddingTop: '20px',
    alignItems: 'center',
  },
  containerGrafic: {
    margin: 'auto',
  },
  typography: {
    textAlign: 'center',
  },
}));
export default usesStyles;
