import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#512da8',
    },

    secondary: {
      main: '#f57c00',
    },
    text: {
      primary: '#ff9800',
    },
  },
});
export default theme;
